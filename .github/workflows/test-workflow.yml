name: example
on:
  issues:
  workflow_dispatch:
    inputs:
      hackme:
        type: string
        default: "safe"

env:
  HACKME_ENV: hello

jobs:
  vulnerable-1:
    runs-on: ubuntu-latest
  
    steps:
      - name: vulnerable-inputs
        run: |
          echo "doing a thing: ${{ inputs.hackme }}"
          
